# <Lead Name>

> **Airtable:** https://airtable.com/appaBExqyEZykb1Qk/tblWNogLfDoppYF1l/<recordId>
> **Site:** <site url> · **Profile:** <profile url>
> **Walked:** YYYY-MM-DD · **Slug:** `<slug>`

---

## Overview

One short paragraph. Name, business, coach type, city, platform, main offer, audience size.
Human context that doesn't fit in a field — who they actually are, what they're selling, what
league they're in. This is the paragraph that stops a future session from under-pricing them
or misreading the lead.

## Funnel Walk

One line per stop where something was found or ruled out. Skip clean, unremarkable stops.

Format: `Stop X (name) — [what's there] — [what it means]`

- Stop 1 (bio link) — …
- Stop 2 (landing) — …
- Stop 3 (opt-in) — …
- Stop 4 (sales/checkout) — …
- Stop 5 (booking) — …

## Evidence

Always three lines. The vision-gate line is the tool's own output, never a paraphrase —
"screenshots read" without the literal line is a false statement.

- **Site vision pass:** `VISION PASS: COMPLETE — N of N required images confirmed read`
- **Pasted evidence:** <what Haytham pasted/attached, one line> OR `none — crawl-only walk`
- **Screenshots:** [`./evidence/`](./evidence/) — see per-finding files below

## Gates

- **Gate 0:** Pass / Fail — <which of the four floors, and why: UAE-based · has funnel · 30-day activity · 1,500 audience>
- **Gate 1:** Pass / Fail — <solo-operator test: is a team/agency/assistant gatekeeping?>
- **Lane:** Lane 1 (felt leak) / Lane 2 (no leak) / Lane 3 (skip)

## Findings — reasoning

> The **canonical, structured** copy of each finding lives in Airtable (`Findings` table):
> rank, status, depth, type, innocent explanation, evidence path. This section carries the
> *reasoning* — why it's deep vs shallow, why it stings, what was ruled out. Don't duplicate
> the fields here; they will drift.

**Rank 1 — <finding, one felt-cost phrase>**
Depth: DEEP / SHALLOW · Type: <finding type>
Innocent explanation: <the non-accusatory framing that makes the opener land>
Why it matters: <the reasoning — what it costs them, why it isn't self-fixable>
Evidence: [`finding-1.png`](./evidence/finding-1.png)

**Rank 2 — <finding>** *(RESERVED — call bait, never emailed)*
…

## SMYKM Hook

`<hook line>` — **<WORK | LIFE | METRIC>** — source: <citation url> or [`hook.png`](./evidence/hook.png)

If no hook was found: `no hook found in public evidence — draft opens on the finding alone`.
That is a valid resolution, not a failure.

---

*Email history lives in Airtable (`Touches` table) — every send and reply, verbatim.
Price discovery answer and anchor live on the Airtable `Leads` record.
Neither is duplicated here.*
