"""outbound — the test-scale Smartlead pipeline.

Deliberately empty, exactly like audit/__init__.py: it exists so tests can
`import outbound.main`, not to re-export anything. See outbound/main.py for
what this path is, why it is separate from the Gmail one, and what it does not
do.
"""
